<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>BookingUni</title>
    <link rel="stylesheet" href="../css/style.css">
</head>

<body>

    <div id="container">

        <nav>
            <div class="left-container">
                <ul>
                    <li><a href="/">Home</a></li>

                    {{#if userId}}
                    <li><a href="/hotels/add">Add +</a></li>
                    {{/if}}
                </ul>
            </div>
            <div class="right-container">
                {{#if userId}}
                <a href="/users/details/{{userId}}" class="log-out">{{username}}</a>
                <a href="/users/logout" class="log-out">Logout</a>
                {{else}}
                <a href="/users/login" class="login">Login</a>
                <a href="/users/register" class="register">Register</a>
                {{/if}}
            </div>
        </nav>
        <main>
            {{#if error}}
            <div class="notification infoBox">
                <span>{{error}}</span>
            </div>
            {{/if}}
            {{{body}}}
        </main>

        <footer>@SoftUni - JS Back-end - BookingUni 2020</footer>
    </div>

</body>

</html>